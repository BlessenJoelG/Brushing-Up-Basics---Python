"""Modules in python are a collection of program files that are created to be reused in programs rather than to write them again and again"""
# # their are two types of Modules
# 1)Built-in Modules : preintalled with the python software
# 2)External Modules : user defined modules
import numpy 
import pandas
import os
import tensorflow